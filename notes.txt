Кнопка "Добавить в корзину" запускается по классу: addCart
Скрипт кнопки вот тут:
frontend\views\blocks\basket.php 187
$('body').on('click', '.addCart',

По классу addCart, есть ещё скрипт тут:
frontend\assets\main\js\function.js 250
$('.addCart').on('click', function ()
там нет ajax запросов, но там что то происходит,
надо разобраться что именно там делается.

PHP скрипт, добавления в корзину находится тут:
frontend\components\CartAction.php 89
public function addCart($id, $count){...}

=======================================================

По электронной оплате:

Кнопка "Подтвердить заказ" находится тут:
frontend\views\site\order.php 414
<button class="btn_Form blue" type="submit">Подтвердить заказ</button>

Всё это сабмитится по action'у формы:
на строке 88
'action' => Url::to(['site/send-form', 'f' => 'order']),

js ajax форма запускается тут:
frontend\assets\main\developer\js\ajax.activeForm.js 202

Далее скрипт идет сюда:
frontend\controllers\SiteController.php
    ->public function actions()
    ->'send-form'=> параметр: 'forms' => 'order' => 'Order'

далее сюда:
frontend\components\SendFormAction.php->public function run($f)

Далее на 28 строке формируется строка класса frontend\\form\\Order

И на 39 строке запускается: $result = $form->send();

Всё это продолжается тут:
frontend\form\Order.php->public function send()

js скрипт формы оплаты онлайн начинается на 646 строке:
var widget = new cp.CloudPayments();

Далее разбираемся дальше ...